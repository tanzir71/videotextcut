# VideoTextCut Dependencies

# IMPORTANT: FFmpeg is required for video processing
# FFmpeg is not a Python package and must be installed separately:
#
# Windows:
#   - Using Chocolatey: choco install ffmpeg
#   - Using Scoop: scoop install ffmpeg
#   - Manual: Download from https://ffmpeg.org/download.html
#
# macOS:
#   - Using Homebrew: brew install ffmpeg
#
# Linux:
#   - Ubuntu/Debian: sudo apt install ffmpeg
#   - CentOS/RHEL: sudo dnf install ffmpeg
#
# After installing FFmpeg, restart your terminal/IDE

# Core dependencies
openai-whisper>=20231117
moviepy>=1.0.3
numpy>=1.21.0
scipy>=1.7.0

# Audio processing
pyaudio>=0.2.11
librosa>=0.9.0
soundfile>=0.10.0

# GUI (tkinter is built-in with Python)
# tkinter - included with Python standard library

# Utility libraries
pathos>=0.3.0
tqdm>=4.64.0
typing-extensions>=4.0.0

# Optional: For better performance
torch>=1.13.0
torchaudio>=0.13.0

# Development dependencies (optional)
# pytest>=7.0.0
# black>=22.0.0
# flake8>=5.0.0